/* @flow */

import Icons from '../../design/icons';

import createCard from '../createCard';
import { Template } from './Metadata';

// TODO: Support red text in part of the title.
export default createCard(Template, Icons.Phone);
